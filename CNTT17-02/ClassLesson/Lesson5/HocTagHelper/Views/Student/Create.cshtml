@model HocTagHelper.Models.Student

@{
    ViewData["Title"] = "Create";
}
<!--HTML Helper-->
@*<div class="container col-md-8">
    <h3>Create New Student</h3>
	@{Html.BeginForm("Save", "Student", "Post");}
        <div class="mt-3">
            @Html.Label("Id","Student Code")
		    @Html.TextBox("Id", Model.Id, new { @class = "form-control" })")
        </div>
        <div class="mt-3">
            <label for="Name">Full Name Student</label>
            <input type="text" id="id" name="id" class="form-control" />
        </div>
        <div class="mt-3">
            <label for="Address">Address</label>
            <input type="text" id="id" name="id" class="form-control" />
        </div>
		<button type="submit" class="btn btn-primary mt-3">Create</button>
	@{Html.EndForm();}
</div>*@

@*<div Class="container col-md-8">
    <h3>Thêm mới sinh viên </h3>

    @{
        Html.BeginForm("Save", "Student", "Post");
    }
    <div class="mt-3">

		@Html.LabelFor(x => x.Id, "Mã sinh viên")
		@Html.TextBoxFor(x => x.Id, new { Type = "number", @class = "form-control" })
        <div class="mt-3">
            @Html.LabelFor(x => x.Name, "Họ Tên")
            @Html.TextBoxFor(x => x.Name, new { Type = "text", @class = "form-control" })
        </div>
        <div class="mt-3">
            @Html.LabelFor(x => x.Address, "Địa Chỉ")
            @Html.TextBoxFor(x => x.Address, new { Type = "text", @class = "form-control" })
        <button type="submit" class="btn btn-danger mt-3">Thêm mới</button>
        @{
            Html.EndForm();
        }
 </div>*@

@*<div class="container col-md-8">
    <h3>Thêm mới sinh viên</h3>

    <form asp-action="Save" asp-controller="Student" method="post">
        <div class="mt-3">
            <label asp-for="Id" class="form-label">Mã sinh viên</label>
            <input asp-for="Id" type="number" class="form-control" />
        </div>

        <div class="mt-3">
            <label asp-for="Name" class="form-label">Họ Tên</label>
            <input asp-for="Name" type="text" class="form-control" />
        </div>

        <div class="mt-3">
            <label asp-for="Address" class="form-label">Địa Chỉ</label>
            <input asp-for="Address" type="text" class="form-control" />
        </div>

        <button type="submit" class="btn btn-danger mt-3">Thêm mới</button>
    </form>
</div>
*@

